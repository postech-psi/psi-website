# Logo Images

Place your logo images in this directory:

- `logo-banner.png` - Horizontal banner logo (PSI with "POSTECH AeroSpace Initiatives" text)
- `logo-circle.png` - Circular logo with PSI emblem (optional, for use in other areas)

The header will automatically use `logo-banner.png`. If the image is not found, it will fall back to text.

